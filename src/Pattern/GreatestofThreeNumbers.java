package Pattern;

public class GreatestofThreeNumbers {
    public static void main(String[] args) {
        int a = 2;
        int b = 34;
        int c = 45678;
//        int ans = (a > b) ? (a > c) ? a : (b > a) ? ( b > c) ? b : (c > a) ? (c > b) ? c : 0 :
    }
}
